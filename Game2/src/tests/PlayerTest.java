package tests;
import univers.*;

/*import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

class PlayerTest {


	@Test
    void testSubirDegats() {
        Player player = new Player("TestPlayer");
        player.subirDegats(20);
        assertEquals(80, player.getPointsDeVie(), "Les points de vie ne sont pas corrects après avoir subi des dégâts.");
    }

    @Test
    void testEstVivant() {
        Player player1 = new Player("PlayerVivant");
        assertTrue(player1.estVivant(), "Le joueur devrait être vivant initialement.");

        Player player2 = new Player("PlayerMort");
        player2.subirDegats(100);
        assertFalse(player2.estVivant(), "Le joueur ne devrait pas être vivant après avoir subi des dégâts mortels.");
    }

    @Test
    void testAttaquer() {
        Player player = new Player("Attaquant");
        assertTrue(player.attaquer(30, 10), "L'attaque devrait réussir avec des dégâts positifs.");

        assertFalse(player.attaquer(10, 30), "L'attaque ne devrait pas réussir avec des dégâts négatifs.");
    }

    @Test
    void testDefendre() {
        Player player = new Player("Défenseur");
        assertDoesNotThrow(() -> player.defendre(), "La défense ne devrait pas provoquer d'exception.");
    }

}
*/
